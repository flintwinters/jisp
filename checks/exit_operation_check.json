[
  {
    "description": "Exit operation - stops execution and returns current state.",
    "jisp_program": {
      "variables": {
        "my_var": "hello"
      },
      "code": [
        ["push", 1.0],
        ["push", 2.0],
        ["set", "another_var"],
        ["exit"],
        ["push", "should_not_reach"]
      ]
    },
    "validation_schema": {
      "type": "object",
      "properties": {
        "stack": {
          "type": "array",
          "minItems": 1,
          "maxItems": 1,
          "items": {
            "type": "number",
            "const": 1.0
          }
        },
        "variables": {
          "type": "object",
          "properties": {
            "my_var": { "type": "string", "const": "hello" },
            "another_var": { "type": "number", "const": 2.0 }
          },
          "required": ["my_var", "another_var"]
        }
      },
      "required": ["stack", "variables"]
    }
  }
]